-r requirements.txt
pytest>=8.0,<9
# Календарь сессий NYSE. Нужен ТОЛЬКО для генерации таблицы
# data/meals/sessions/nyse.csv (python -m meals.sessions), а не в часовом
# прогоне: он читает готовый CSV. Держать его здесь, а не в requirements.txt,
# значит не тащить календарную библиотеку и её зависимости в продакшен и не
# рисковать тем, что её обновление молча изменит историческое расписание.
exchange-calendars>=4.13,<5
