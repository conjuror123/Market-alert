requests>=2.31,<3
PyYAML>=6.0,<7
# MEALS: векторный слой расчёта метрик и колоночное хранилище баров
# (ТЗ 5.1 п.6.1). Отдельной библиотеки под PCA не понадобилось: корреляционная
# матрица корзины - это 21 на 21, и numpy.linalg.eigh считает её быстрее, чем
# scikit-learn успел бы проверить входные данные.
pandas>=2.2,<3
pyarrow>=16
