requests>=2.31,<3
PyYAML>=6.0,<7
# Tremor: the vectorised layer for computing metrics and the columnar bar store.
# No separate linear-algebra library is needed at this size -
# numpy solves the basket's matrices faster than scikit-learn would take to
# validate the input.
pandas>=2.2,<3
pyarrow>=16
